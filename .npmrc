; Guard: this branch publishes the 3.0.0-beta.x line. Default every publish
; from this checkout to the npm `beta` dist-tag so `latest` stays on 2.x.
; (publishConfig.tag is not honored by all npm versions; this is.)
tag=beta
